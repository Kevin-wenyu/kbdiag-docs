# Round 3 validation

- Replication baseline: both roles verified, sender/receiver streaming, receive/replay LSN 0/A00117E8. Byte gap zero while standby transaction-age display was about 43 seconds. No deliberate replay pause or failover.
- Report: pre-repair lab capture at 2026-09-16 08:38 UTC+08; exit 2, 56 OK / 16 WARN / 1 FAIL. All eleven sections preserved. Host/address/path/object/license/customer identifiers replaced; counts independently reconciled. Raw file is not published.
- Source artifact: kbdiag commit 3bea0beeaf6739083f431158c98ca4ee89657767, embedded v1.0.0-115 (2026-08-10).
- Strict Hugo build passed. 43 HTML pages, 1928 internal link checks, zero failures.
- Seven validator fixture tests passed, including broken targets, missing fragments, missing translation, evidence markers and stale search targets.
- Workflow validates pull requests and main pushes; deploy job restricted to main non-PR runs. Deployment permissions scoped to the deploy job.
- These tests validate documentation consistency and sampled lab behavior, not production readiness or restore capability.

## Archive repair closure
- Root cause: repository-node SSH host key no longer matched the primary user's known_hosts. Verified the current ECDSA public key directly via Lima before updating the specific host entry. StrictHostKeyChecking remains enabled. Original known_hosts backed up.
- Additional check configuration fix: primary sys_rman stanza lacked kb1-port and kb1-user. Set port 54321 and existing backup role esrep after verifying command-line overrides. Original configuration backed up.
- Persistent-config sys_rman check succeeded and confirmed WAL ...A2 archived to the repository.
- Primary archived_count=35, failed_count=1422 (stable); last success newer than last failure. Both nodes had zero .ready files. Standby archived_count=35, failed_count=0; receive/replay equal. Primary still streaming/quorum with 0 bytes gap.
- Historical failures retained; no WAL files or counters deleted/reset. No full restore performed. VM clocks changed after startup time synchronization; retain literal timestamps from each capture and use WAL identities/counts as closure evidence.
