# 第一轮文档改版验收记录

日期：2026-09-15

## 结论

内容和网站构建验证通过，可以进行人工预览。本记录写于发布前；真实数据库采集、异常场景实机验证和陌生用户阅读测试待完成。

## 功能依据

核对的 kbdiag 提交：`3bea0beeaf6739083f431158c98ca4ee89657767`。以下是源码核实，不等同于本轮实机验证。

| 说明 | 依据 | 状态 |
|---|---|---|
| check 的连通性失败返回 2；健康检查合并最差等级 | lib/cmd_check.sh:7、15、326 | 源码已核实 |
| Waiting locks 统计锁记录，不等于会话数 | lib/cmd_check.sh:104 附近的 sys_locks 查询 | 源码已核实 |
| report 汇总检查并写 Markdown 文件 | lib/cmd_report.sh:44 起 | 源码已核实 |
| advisor --fix 生成 SQL | lib/cmd_advisor.sh:97 起的生成分支 | 源码已核实 |
| workload 的可选补快照、只读节点限制、15 分钟回退 | lib/cmd_workload.sh:44 起 | 源码已核实 |
| snapshot 是现场采集，字面量遮盖不等于完整脱敏 | lib/cmd_snapshot.sh:1–31 | 源码已核实 |
| kill 默认取消，--terminate 终止 | lib/cmd_kill.sh:8–17、85–87 | 源码已核实 |
| 默认连接参数与目录探测 | lib/core.sh:1–36 | 源码已核实 |
| --version 支持 | kbdiag.sh:107 | 源码已核实 |
| stmt 依赖 sys_stat_statements | lib/cmd_stmt.sh:8–28 | 源码已核实 |

源码入口：https://github.com/Kevin-wenyu/kbdiag/tree/3bea0beeaf6739083f431158c98ca4ee89657767/lib

## 网站验证

- Hugo Extended 0.165.0，严格生产构建通过，无警告。
- 31 个 HTML 输出文件，1174 个站内链接、资源引用和锚点检查，0 个失败。
- 已修复结果解读页返回巡检章节的相对锚点；使用 Hugo relref 解析。
- 已保留原英文和中文 reference 路由，只调整导航排序。
- 浏览器中搜索 `check` 返回第一次巡检、功能概览等 6 个结果，点击后进入正确页面。
- 首次巡检中文 → 英文切换到对应页面，不回首页。
- 桌面首页、390×844 手机首页及文档页面已查看。手机页 document.scrollWidth=375，innerWidth=390，无整页横向溢出；长代码在代码块内部滚动。
- 中英文页面使用同样的明确标题锚点。

构建命令（网站根目录）：

```bash
hugo --gc --minify --environment production --printPathWarnings --panicOnWarning
```

## 真实采集状态

`limactl list` 显示 kes-node1、kes-node2 都为 Stopped。本轮未启动数据库测试节点，也未执行数据库命令。站点中的命令块是用法说明；结果解读页明确标注“虚构数值的示意节选”，没有声称采自真实环境。

待补充：

1. 在测试节点采集 `--version`、数据库版本、节点角色、时间、`status` 和 `check` 原始输出与退出码。
2. 脱敏并复核后，用真实节选补充示意页，保留采集来源。
3. 验证连接失败、正常、WARN 路径；需要注入异常时限定在测试环境。
4. 请未接触项目的读者完成首次巡检阅读任务，记录卡点。
5. 用户确认预览后发布，并检查线上搜索、语言切换、静态资源及链接。

## 改动范围

- 中英文首页：项目定位、命令使用路径、六类功能、看查断、常见问题和快速开始入口。
- 新增认识项目、功能概览、结果解读页。
- 扩展首次巡检：安装、连接、多实例、退出码和下一步。
- 保留现有命令手册正文、部署工作流和主题版本。
