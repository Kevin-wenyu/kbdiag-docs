# Documentation completion and lab archiving repair

**Goal:** Complete the three approved website improvements and resolve the independent local-lab archive failure.
**Architecture:** Keep OINK layouts; capture lab evidence; run a standard-library Python validator before Pages upload. Diagnose and repair archive configuration independently of website content.
**Tech Stack:** Hugo 0.165.0, OINK v1.0.0, Python 3, KingbaseES V008R006C009B0014.
**Spec:** User request: replication guide, report example, automated documentation checks, then fix the lab archive issue.

## Constraints
- No invented output, no resetting counters to conceal history, no deleting WAL files.
- Keep raw reports and infrastructure configuration out of the public site.
- No failover or deliberate interruption of replication required for documentation.
- Publish after local validation; verify the deployed revision and pages.

## Tasks
- [x] Capture `replication -v --exit-code` on both roles and independently query sender/receiver LSNs. Write bilingual `content/docs/scenarios/replication-lag{,.zh}.md` with idle-time caveat and evidence boundary.
- [x] Generate `report` into a private temporary path; review/sanitize the downloadable sample, preserve all findings, validate summary counts. Add bilingual `content/docs/get-started/report-example{,.zh}.md` and navigation links.
- [x] Add `scripts/check-site.py`: links, anchors, bilingual core pages, search-index targets and required evidence markers. Add fixture tests for broken paths, anchors and missing translations. Run before artifact upload; PRs validate without deploying.
- [x] Read archive command, target permissions, log errors and archiver counters. Back up changed configuration; apply the minimum fix; force a bounded WAL switch and verify archive bytes, newer successes, stable failure counter, drained queue and continuing replication. Retain historical warnings.
- [x] Strict build and negative tests; publish docs and verify online. Record repair evidence and restore VMs to stopped state.
