# Round 2: evidence and troubleshooting guides

Approved scope: replace illustrative inspection output with a real test capture; add bilingual slow SQL and lock-wait guides. Keep OINK layouts and existing routes.

1. Capture baseline health output and exit code from the local KingbaseES lab.
2. Reproduce bounded sleep and row-lock waits in dedicated sessions; verify against system views and clean up.
3. Publish reviewed output excerpts with provenance and limitations, never raw private captures.
4. Add scenario navigation and matching English/Chinese pages.
5. Strict Hugo build, internal link checks and browser preview before publication.

Acceptance: commands match this tool revision; failures remain visible; synthetic scenarios are labeled; no test sessions or objects remain. Documentation build success is not production validation.
